# Public base URL the download buttons build artifact links from.
# Optional — defaults to https://resource.oh-my-github.app when unset.
# Copy to .env for local overrides (git-ignored).
VITE_R2_PUBLIC_BASE_URL=https://resource.oh-my-github.app
